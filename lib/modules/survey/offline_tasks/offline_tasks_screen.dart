import 'package:flutter/material.dart';

class OfflineTasksScreen extends StatelessWidget {
  const OfflineTasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Offline Tasks"),
      ),
    );
  }
}
